export type ChampionNameProps = {
  championNamestyle: string;
};

const ChampionName = (props: ChampionNameProps) => {
  return <span style={{ fontSize: "15px", padding: "10px 7px" }}>비에고</span>;
};

export default ChampionName;
